array = {1, 3, 5, 4, 5, 9, 6, 1, 2, 6, 5, 4, 4, 9, 7 }
index of array shall be i 
variable x is integer
variable i is integer
variable v1 and v2 are integer and start as 0
x=0; i=0
int maxn 
int maxo
=======variable define outside of loop
while i is not the last item of the array increase i by one for each each iteration    
    while maxn is 0
        v1 = value of i, v2= value of next i
        if v1 - v2 is negative
            maxn = v2;
        else maxn = v1;
    while maxn > 0 
        if maxn-v2 not negative  maxn=maxn
        if maxn-v2 = 0 maxn=maxn
        else maxn =v2



